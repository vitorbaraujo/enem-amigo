<!--
 File: result.html.erb
 Purpose: File that shows the result of a battle
 License : LGPL. No copyright.
-->

<h1>
  Resultado da Batalha
</h1>

<h2>
  <% full_winner_name = @battle.winner.name + " (" + @battle.winner.nickname + ")" %>
  Vencedor: <%= full_winner_name %>
</h2>

<table class='table'>
  <thead>
    <th class="align-center">Gabarito</th>
    <th class="align-center">Suas Respostas</th>
    <th class="align-center">Respostas do Adversário</th>
  </thead>

  <% @answers.each do |feedback, user_answer, adversary_answer| %>
    <tr>
      <td class="align-center"><%= feedback.right_answer %></td>
      <td class="align-center"><%= user_answer %></td>
      <td class="align-center"><%= adversary_answer %></td>
    </tr>
  <% end %>
</table>

<h2>
  Questões acertadas por você: <%= @current_player_stats.first %> (Tempo: <%= @current_player_stats.second %>)
</h2>
<h2>
  Questões acertadas pelo adversário: <%= @adversary_stats.first %> (Tempo: <%= @adversary_stats.second %>)
</h2>